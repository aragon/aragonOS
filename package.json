{
  "name": "@aragon/os",
  "version": "3.0.5",
  "description": "Core contracts for Aragon",
  "scripts": {
    "test": "npm run manage-nodes:dev && truffle test --network rpc",
    "test:geth": "export GETH_CLIENT=geth; npm run manage-nodes:dev && truffle test --network rpc",
    "test:parity": "export GETH_CLIENT=parity; npm run manage-nodes:dev && truffle test --network rpc",
    "test:gas": "GAS_REPORTER=true npm test",
    "lint": "solium --dir ./contracts",
    "coverage": "npm run manage-nodes:coverage && node_modules/.bin/solidity-coverage",
    "console": "node_modules/.bin/truffle console",
    "manage-nodes:dev": "scripts/manage-nodes.sh",
    "manage-nodes:coverage": "SOLIDITY_COVERAGE=true scripts/manage-nodes.sh",
    "migrate:dev": "npm run manage-nodes:dev && npm run migrate:dev:contracts",
    "migrate:dev:contracts": "truffle migrate --all --network rpc;",
    "generate:artifacts-dev": "npm run migrate:dev; truffle exec --network rpc scripts/generate_artifacts.js",
    "cleanup:docker": "docker stop $(docker ps -aq)"
    "prepublishOnly": "truffle compile --all"
  },
  "files": [
    "index.js",
    "build/",
    "artifacts/",
    "contracts/",
    "migrations/",
    "truffle-config.js"
  ],
  "author": "Aragon Institution MTU <contact@aragon.one>",
  "contributors": [
    "Jorge Izquierdo <jorge@aragon.one>"
  ],
  "license": "GPL-3.0",
  "devDependencies": {
    "coveralls": "^2.13.3",
    "eth-ens-namehash": "^2.0.8",
    "eth-gas-reporter": "^0.1.1",
    "ethereumjs-abi": "^0.6.5",
    "ganache-cli": "^6.0.3",
    "glob": "^7.1.2",
    "homedir": "^0.6.0",
    "mocha-lcov-reporter": "^1.3.0",
    "solidity-coverage": "^0.3.5",
    "solium": "^1.1.2",
    "truffle": "4.0.5",
    "truffle-config": "^1.0.4",
    "truffle-hdwallet-provider": "0.0.3"
  }
}
